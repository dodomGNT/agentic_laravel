# Claude Config

This project uses spec-driven agentic workflow.
See `AGENTS.md` for full reference.

Commands are in `.claude/commands/`:
- `/spec` — write PRD
- `/plan` — architecture + stories
- `/code` — implement story
- `/review` — review code
- `/test` — test code
